{
  "name": "create-rust-wasm-app",
  "version": "1.0.0",
  "description": "Create a Rust WASM app in seconds.",
  "scripts": {
    "setup": "./setup",
    "server": "node server.mjs",
    "build": "wasm-pack build --target web --out-name wasm --out-dir static/wasm"
  },
  "author": "Koen Vendrik <k.vendrik@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "koa": "^2.13.0",
    "koa-static": "^5.0.0",
    "node-watch": "^0.6.4"
  },
  "engines": {
    "node": ">13"
  }
}
